/**
 * Справочник для LLM-ассистента в дашборде админки (system prompt).
 * Не Cursor Skill — подмешивается в ASSISTANT_SYSTEM_PROMPT при каждом чате.
 */
export const ASSISTANT_KNOWLEDGE_USER_GROUPS = `
## Справочник: группы пользователей (User Groups)

Раздел админки: **Настройки → Группы пользователей** — \`/admin/settings/user-groups\`.
ACL: grant **«Пользователи»** (\`users\`). Legacy \`/admin/user-groups/*\` → redirect.

Группы **не синхронизируются с 1С**. Один покупатель — одна кастомная группа (\`User.groupId\`) или авто-контекст.

### Типы групп

| Бейдж | Slug (seed) | Кто попадает |
|-------|-------------|--------------|
| **Гости** | \`guests\` | Все без входа (авто, \`isDefaultGuest\`) |
| **Розница** | \`retail-registered\` | Зарегистрированные **без** явной группы (авто, \`isDefaultRegistered\`) |
| **Кастомная** | свой slug | Только назначенные участники (\`assignable: true\`) |

Системные группы нельзя удалить; slug системных не меняется. Неактивная кастомная группа у пользователя → fallback в **Розницу**.

### Карточка группы (\`/admin/settings/user-groups/[id]?tab=\`)

| Вкладка | Содержание |
|---------|------------|
| **Общее** | Название, slug, округление, флаги промо, активность, удаление (не для системных) |
| **Участники** | Только кастомные (\`assignable\`); также назначение в \`/admin/users/[id]\` |
| **Цены** | Чипы **Категории** и **Товары** (\`?tab=prices&section=products\`) |
| **Видимость** | Кто видит товар/категорию/вариант |

### Округление (\`priceRounding\`)

Применяется к правилам категории **−%** и **−₽** (не к фикс SKU и не к «Фикс ₽» категории): NEAREST / FLOOR / CEIL.

### Стек цены на витрине

1. **База** варианта в каталоге
2. **Групповая цена** (всегда): SKU override → ближайшее правило категории вверх по дереву → база
3. **[если \`allowCatalogDiscounts\`]** кампании **Discount** (каталожные акции из раздела Discount)
4. **[если \`allowPromoCodes\`]** промокод на checkout (от subtotal корзины)

Групповые цены **не отключаются** чекбоксами ниже — они управляют только пунктами 3–4.

#### «Кампании Discount (каталожные акции)» — \`allowCatalogDiscounts\`

- **Вкл.:** после групповой цены накладываются активные кампании Discount (каталог, PDP, корзина, избранное).
- **Выкл.:** кампании Discount для этой группы не применяются; остаётся только групповая цена.

#### «Промокоды на checkout» — \`allowPromoCodes\`

- **Вкл.:** поле промокода на checkout, validate + скидка от subtotal.
- **Выкл.:** промокод недоступен («Промокоды недоступны для вашей группы»). Подарочный сертификат обычно остаётся.

Seed по умолчанию: оба **вкл.** у Гостей и Розницы.

### Вкладка «Цены»

**Категории** — только **leaf**-категории (без подкатегорий):
- **−%** (\`PERCENT_OFF\`), **−₽** (\`FIXED_OFF\`), **Фикс ₽** (\`FIXED_PRICE\`)
- На витрине правило наследуется вниз; при расчёте ищется **ближайшее правило вверх** от leaf.
- Скидку на всю ветку → правило на **каждый leaf** (родительскую категорию в UI цен задать нельзя).

**Товары** — один товар, все его варианты:
- **−%** — скидка ко всем SKU (сохраняется как вычисленная фикс-цена на вариант)
- **Фикс ₽** — цена по вариантам
- Приоритет SKU выше правил категории.

**Витрина:** групповая цена показывается в \`price\` на карточке и PDP. Зачёркнутая «старая» база (\`oldPrice\`/\`compareAt\`) **не появляется автоматически** от правила группы — только если задан \`compareAt\` в каталоге или сработала кампания Discount.

**ISR:** каталог/PDP сначала guest shell, после login — client refetch с JWT покупателя.

### Вкладка «Видимость»

| Режим | Смысл |
|-------|--------|
| Скрыть от гостей | Видят только зарегистрированные |
| Скрыть от зарег. | Видят только гости |
| Скрыть от группы | Скрыто участникам этой группы |
| Только зарег. | Whitelist: только залогиненные |
| Только группа | Whitelist: только эта группа |

Несколько правил комбинируются (whitelist сужает, hide вычитает). Category visibility матчит **ancestors**; VARIANT — на уровне SKU.

### Типовые сценарии (инструкции оператору)

**−10% всем зарегистрированным без кастомной группы:**
1. Открыть группу **«Розница (зарег.)»** (\`retail-registered\`), не кастомную.
2. Цены → Категории → **−10%** на каждую leaf-категорию.
3. Группа должна быть **активна**.

**Закрытый B2B-прайс:**
1. Создать кастомную группу, назначить участников.
2. Цены → категории/товары; при необходимости выключить Discount и промокоды.
3. Видимость → «Только группа» на эксклюзивные SKU.

**Акция Discount только для гостей:**
1. У **Гостей** — \`allowCatalogDiscounts\` вкл.; у **Розницы** — выкл. (или другие групповые цены).
2. Кампанию создать в разделе Discount.

### Tools ассистента (read-only, ACL «Пользователи»)

| Tool | Назначение |
|------|------------|
| \`list_user_groups\` | Список групп: kind guest/retail/custom, флаги, счётчики |
| \`get_user_group\` | Одна группа по \`groupId\` или \`slug\`; \`include\`: category_prices, variant_prices, visibility, members |

Примеры: «есть ли −10% у розницы» → \`get_user_group\` slug=retail-registered, include=[category_prices]. «Сколько SKU-цен у опта» → \`list_user_groups\` q=opt.

**Не предлагай сам менять группы в UI** — только факты из tools + инструкции оператору.
`.trim();
